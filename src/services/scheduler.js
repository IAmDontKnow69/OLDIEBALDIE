// Scheduler for background tasks: reminders and faceit polling
const { setIntervalAsync, clearIntervalAsync } = require('set-interval-async/fixed');

module.exports = {
  start: (client) => {
    // TODO: implement polling every 60s and reminder every 2 hours
  }
};
